//only to add/remove participants
//Needs only POST actions:
//Add(appointmentId, userId) - adds participant if not exists
//Remove(appointmentId, userId) - removes participant if exists
//redirect: back to Appointments/Details/{appointmentId}